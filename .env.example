# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME="Ravamet API"
PROJECT_DESCRIPTION="API for job vacancies and responses"
PROJECT_VERSION=0.1.0

# CORS Configuration
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080"]

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SERVER=localhost
POSTGRES_DB=ravamet
# Uncomment to override the constructed database URL
# DATABASE_URL=postgresql://postgres:postgres@localhost/ravamet

# JWT Configuration
SECRET_KEY=change-this-key-in-production-use-openssl-rand-base64-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Server Configuration
HOST=0.0.0.0
PORT=8000
RELOAD=true
